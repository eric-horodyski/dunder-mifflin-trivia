import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.horodyski.dmt',
  appName: 'dunder-mifflin-trivia',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
